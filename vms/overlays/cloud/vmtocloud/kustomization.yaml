# Cloud/DR cluster overlay.
# Contains VM objects ONLY — no PVCs (VolumeSync ReplicationDestination owns them).
# VMs start halted by default; failover-control component flips them to running.
# AUTO-GENERATED by AAP "VM DR - Reconcile VMs" playbook.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: vmtocloud

resources: []
# vm-<name>.yaml entries added here by the reconciler (no pvc-*.yaml).

components:
  - components/failover-control
